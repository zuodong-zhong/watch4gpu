# Contributing

感谢你改进 GPU Watch。

## 开发流程

1. Fork 仓库并创建独立分支。
2. 使用 `npm ci` 安装依赖。
3. 保持改动聚焦，并为行为变化补充测试。
4. 提交前运行 `npm run lint` 和 `npm test`。
5. 在 Pull Request 中说明改动原因、用户影响和验证方式。

## 隐私要求

不要提交真实服务器 IP、SSH Host 别名、用户名、姓名映射、组织内部路径、密码、私钥、令牌或 `connect.exp`。示例网络地址请使用 RFC 5737 保留地址（例如 `192.0.2.10`），示例身份请使用虚构名称。

如果不确定某项信息是否适合公开，请先删除或替换为占位符。
